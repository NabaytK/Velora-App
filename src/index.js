import React from "react";
import ReactDOM from "react-dom";
import App from "./app/App"; // Ensure App.js exists in /app

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
